import { StatusTaskPipe } from './status-task.pipe';

describe('StatusTaskPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusTaskPipe();
    expect(pipe).toBeTruthy();
  });
});
